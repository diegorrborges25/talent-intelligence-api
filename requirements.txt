# Talent Intelligence API — dependências de runtime (núcleo)
# Tudo aqui é "pure-python" ou possui wheel pré-compilada (inclusive para Python 3.14),
# portanto a API roda em qualquer máquina apenas com `pip install -r requirements.txt`.
fastapi==0.137.1
uvicorn==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
PyJWT==2.13.0
python-multipart==0.0.32
python-dotenv==1.2.2

# IA / LLM (Claude). A API continua funcionando 100% offline (motor de regras +
# similaridade) caso a chave ANTHROPIC_API_KEY não seja definida no .env — a
# ausência/erro da lib NÃO quebra a aplicação (fallback automático em app/ai/llm.py).
anthropic==0.40.0
